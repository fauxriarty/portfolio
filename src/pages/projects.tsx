import React from 'react';

// since routing in next works directly by adding the .tsx in the
// pages directory. 
export const Projects = () => {
    return <p className='projtext'>Hello, these are my projects.</p>;
  };
  
  export default Projects;